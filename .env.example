# Optional. Defaults to the current user's Codex sessions directory.
# Use a placeholder or local-only value in your own .env file.
# CODEX_SESSIONS_DIR=/path/to/.codex/sessions

# Optional.
PORT=4177
